{
  "name": "json-api",
  "version": "1.0.0",
  "description": "Deployed json-server on Render",
  "scripts": {
    "start": "json-server --watch data/db.json --host 0.0.0.0 --port $PORT"
  },
  "dependencies": {
    "json-server": "^0.17.3"
  }
}
